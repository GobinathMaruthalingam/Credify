{
    "name": "credify-frontend",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "@vercel/speed-insights": "^1.3.1"
    }
}
